# @nexusmods/vortex-api is a types-only dev dependency whose (unused) peer
# dependencies would otherwise force a conflicting React tree. We only need
# the type declarations, so skip peer resolution.
legacy-peer-deps=true
